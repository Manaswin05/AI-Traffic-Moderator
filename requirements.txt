# ─────────────────────────────────────────────────────────────
# AI Traffic Moderator — Python Dependencies
# Runtime: ONNX Runtime CPU (replaces PyTorch ~800 MB → ~50 MB)
# ─────────────────────────────────────────────────────────────

# Web framework
flask==3.0.0
werkzeug==3.0.1
flask-cors==4.0.0

# WSGI server
gunicorn==21.2.0

# ONNX inference (CPU-only, tiny footprint)
onnxruntime==1.17.1

# Computer vision
opencv-python-headless==4.8.1.78

# ML / numerics
numpy==1.26.2
scikit-learn==1.3.2

# ─────────────────────────────────────────────────────────────
# NOTE: PyTorch and ultralytics are used ONLY at build time
# to export yolov8n.pt → yolov8n.onnx (see build.sh).
# They are NOT installed at runtime, saving ~800 MB RAM.
# ─────────────────────────────────────────────────────────────
